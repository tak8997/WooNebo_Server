<%
    layout('/layout')
    stylesheet('/css/kiosk.css')
    script('/js/kiosk.js')
%>
<div>
    <div class="row">
        <div class="menu-name">
            <span class="bold-text">키오스크 관리</span>
            <div class="btn-right">
                <button class="btn btn-primary btn-sm" id="kiosk-new">등록</button>
            </div>
        </div>
    </div>
    <table class="table table-hover">
        <thead>
            <tr class="header">
                <th>ID</th>
                <th>Description</th>
                <th>Location (lat, lng)</th>
                <th>BLE</th>
                <th>Play File</th>
                <th>Play at</th>
                <th>Created at</th>
                <th>Updated at</th>
            </tr>
        </thead>
        <tbody class="table-hover">
        <% kiosks.forEach(function(kiosk) { %>
            <tr class="list" id=<%= kiosk.id %> >
                <th><%= kiosk.id %></th>
                <td><%= kiosk.description %></td>
                <td><%= kiosk.lat %>, <%= kiosk.lng %></td>
                <td><%= kiosk.ble %></td>
                <td><%= kiosk.last_file %></td>
                <td><%= kiosk.last_play_at %></td>
                <td><%= kiosk.create_at %></td>
                <td><%= kiosk.update_at %></td>
            </tr>
        <% }); %>
        </tbody>
    </table>
    <nav class="pagination">
    <% if (locals.prev) { %>
        <a href="/admins/kiosks?page=<%= (locals.page - 1) %>">Previous</a>
    <% } %>

    <% if (locals.next) { %>
        <a href="/admins/kiosks?page=<%= (locals.page + 1) %>">Next</a>
    <% } %>
    </nav>
</div>
